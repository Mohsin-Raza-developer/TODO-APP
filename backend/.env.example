# Neon PostgreSQL Database Connection
# Format: postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require
# IMPORTANT: Use the pooled connection endpoint (with -pooler suffix) for serverless compatibility
DATABASE_URL=postgresql://user:password@ep-xxx-pooler.us-east-2.aws.neon.tech/todo_db?sslmode=require

# Better Auth Secret for JWT Token Verification
# Generate with: openssl rand -base64 32
# IMPORTANT: Keep this secret secure - never commit to git
BETTER_AUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32
